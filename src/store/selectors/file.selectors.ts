// import { createSelector } from 'reselect';
// import { FilePayload } from 'store/actions/file.actions';

export const selectFiles = (state: any) => state.files;

// export const selectUserReceiveTrucksPermission = createSelector(
//   selectFiles,
//   (file: FilePayload) => file,
// );
